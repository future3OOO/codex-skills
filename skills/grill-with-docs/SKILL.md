---
name: grill-with-docs
description: Compatibility wrapper for grilling plus domain-modeling. Use when a grilling session must respect or update CONTEXT.md and ADRs.
---

# Grill With Docs

Use `$grilling` together with `$domain-modeling`.

This wrapper exists for compatibility with older prompts. The questioning loop
lives in `$grilling`; durable terminology, `CONTEXT.md`, and ADR formats live
in `$domain-modeling`.
