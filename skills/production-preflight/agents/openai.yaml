interface:
  display_name: "Production Preflight"
  short_description: "Before-edit proof for production code"
  default_prompt: "Use $production-preflight to produce a compact before-edit proof before risky tracked production changes."
